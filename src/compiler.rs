use crate::scanner::Scanner;


pub struct Compiler {

}

impl Compiler {
    fn compile(&self,source:&String){
        let scanner = Scanner::new(source);
        loop {
            
        }
    }
}